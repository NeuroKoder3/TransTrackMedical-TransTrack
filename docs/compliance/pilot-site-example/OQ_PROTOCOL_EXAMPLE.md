# OQ Protocol — TransTrack Operational Qualification (Worked Example)

> ## ⚠ DEMONSTRATION ONLY — NOT A REAL VALIDATION RECORD
>
> "Northshore Regional Transplant Center" is a **fictional** organization.
> All test results below are **synthetic** demonstration data. See
> [`README.md`](README.md). Replace before treating this file as a real
> validation record. Real pilot sites should execute against the empty
> template at [`../templates/OQ_PROTOCOL_TEMPLATE.md`](../templates/OQ_PROTOCOL_TEMPLATE.md).

---

| Document control | |
|---|---|
| Document ID | TT-OQ-NRTC-001 (example) |
| Software version | **v1.2.0** (build `e1436e2`) |
| Executed by | _Sarah Chen, RN — Validation Lead_ *(role-title placeholder)* |
| Reviewed by | _Marcus Johnson, MS — QA Officer_ *(role-title placeholder)* |
| Date executed | 2026-06-05 *(example)* |

## Purpose

Verify each Mandatory requirement from
`docs/compliance/SYSTEM_REQUIREMENTS_SPECIFICATION.md` against the running
v1.2.0 build. OQ executed in a **non-PHI** test environment seeded with 12
synthetic patients.

> Each "OQ-NN" id below corresponds to a row in
> `docs/compliance/TRACEABILITY_MATRIX.md`.

## Authentication and access control

| ID | Step | Expected | Pass/Fail | Evidence |
|---|---|---|---|---|
| OQ-01 | Attempt login with valid credentials. | Login succeeds; session created. | **PASS** | Screenshot `oq01-login.png`; session row in `sessions` table inspected via System Diagnostics. |
| OQ-02 | Attempt to set password "abc123". | Rejected: complexity not met. | **PASS** | Inline error "Password must be 12+ chars and include uppercase, lowercase, digit, special". |
| OQ-03 | Submit 5 invalid passwords for one account. | Account locked for ≥15 min. | **PASS** | After attempt 5: "Account locked. Try again in 15 minutes." Lockout cleared at 15:32 (was triggered 15:17). |
| OQ-04 | Enroll TOTP MFA; log out; log in with TOTP. | Login succeeds only with valid TOTP. | **PASS** | Enrollment QR scanned via Authy; valid TOTP accepted, invalid rejected. |
| OQ-05 | Use a backup code; verify it cannot be reused. | First use succeeds; second use rejected. | **PASS** | Backup code `2DF7-XK91-...` first use OK; replay returns "Backup code already used". |
| OQ-06 | Attempt to set a password used in last 12. | Rejected: reuse not allowed. | **PASS** | Inline error: "This password was used previously. Choose a different one." |
| OQ-07 | Set password rotation to 1 day; advance system clock 2 days. | User prompted to change password on next login. | **PASS** | Forced password-change screen rendered on next login. |
| OQ-08 | Leave session idle for >15 min. | Session ends; re-auth required. | **PASS** | Idle-timeout dialog at 15:01; re-auth required. |
| OQ-09 | Attempt admin operation as `viewer` role. | Rejected. | **PASS** | "User Management" menu item not visible in viewer role; direct-IPC attempt via dev tooling: `IpcError: insufficient_permissions`. |

## Auditing

| ID | Step | Expected | Pass/Fail | Evidence |
|---|---|---|---|---|
| OQ-20 | Create a patient. | Audit row recorded with action=create, actor, request_id. | **PASS** | Row in `audit_logs`: action=`patient.create`, actor=`coordinator-2`, `request_id=` UUID v4, before=null, after={…}. |
| OQ-22 | Attempt `UPDATE audit_logs SET ...` via direct SQL. | Trigger raises `HIPAA Compliance: Audit logs are immutable`. | **PASS with note** | Trigger fires; row remains unchanged. **Note:** SQLite error string under SQLCipher v4.6 surfaces as `SQLite error: cannot modify audit_logs because it is read-only` rather than the documented `HIPAA Compliance:` prefix. Functional immutability intact. **Logged as DEF-001 in VSR.** |
| OQ-24 | Export patient list to CSV. | Audit row recorded; export file watermarked. | **PASS** | CSV header line: `# DO_NOT_SUBMIT — operational export, not for OPTN/UNet — generated by TransTrack v1.2.0 at 2026-06-05T15:08:14Z`. Audit row recorded. |
| OQ-25 | Change a user's role. | Audit row recorded. | **PASS** | Audit row: action=`user.update`, before={role:`coordinator`}, after={role:`viewer`}. |
| OQ-26 | Configure a SIEM destination; perform an action; observe SIEM. | Event appears in SIEM in CEF format. | **PASS** | CEF line received at `siem.nrtc.local`: `CEF:0\|TransTrack\|TransTrack\|1.2.0\|patient.create\|patient.create\|3\|requestId=…actor=… orgId=…` — PHI fields confirmed absent. |

## Encryption

| ID | Step | Expected | Pass/Fail | Evidence |
|---|---|---|---|---|
| OQ-40 | Inspect database file with `sqlite3`. | Encrypted; opaque. | **PASS** | `sqlite3 transtrack.db ".tables"` → `Error: file is not a database`. |
| OQ-41 | Inspect installation directory for cleartext key. | None present. | **PASS** | `dir /s %APPDATA%\TransTrack\` does not surface any plaintext key file; `.transtrack-key-meta` present but contains only Electron `safeStorage`-wrapped ciphertext. |
| OQ-42 | Run encryption key rotation as admin. | Rotation completes; history row recorded. | **PASS** | Settings → Encryption → Rotate Key: rotation completed in 2.4 s; history row recorded with old-key fingerprint truncated, new-key fingerprint, actor, timestamp. |
| OQ-43 | Corrupt the database file 1 byte; restart. | Integrity check fails; user warned; backup-restore prompt shown. | **PASS with note** | Single-byte flip at offset 1024; on restart: warning dialog "Database integrity check failed. Restore from backup?" Path displayed in OS-locale form. **Logged as DEF-002 in VSR (cosmetic doc delta only).** |
| OQ-44 | Export to PDF. | File header shows producer + timestamp + confidentiality banner. | **PASS** | PDF metadata: Producer=`TransTrack 1.2.0`, Title carries `CONFIDENTIAL — DO NOT DISTRIBUTE`. |

## Operational features

| ID | Step | Expected | Pass/Fail | Evidence |
|---|---|---|---|---|
| OQ-60 | Create patient with all fields. | Stored; retrievable. | **PASS** | Synthetic patient `MRN-NRTC-OQ-001` created and retrieved via list. |
| OQ-61 | Calculator: enter MELD inputs; verify formula matches reference. | Matches `tests/calculators.test.cjs`. | **PASS** | MELD inputs (creatinine 1.2, bilirubin 0.8, INR 1.0) → score 6.4, matches reference. Insufficient-input case ("creatinine missing") returns "Insufficient data". |
| OQ-62 | View Priority Score; confirm "operational not allocative" label. | Label visible. | **PASS** | Priority breakdown panel reads "Operational priority — not for allocation. Allocation occurs in OPTN/UNet." |
| OQ-63 | Create barrier; resolve; audit. | Recorded. | **PASS** | Barrier `INSURANCE` created at 14:22, resolved at 14:24; both transitions audited. |
| OQ-64 | Create AHHQ; mark complete. | Recorded. | **PASS** | AHHQ status `complete`, last_completed_date `2026-06-05`, audit row recorded. |
| OQ-65 | Add lab result; verify stored as string. | Yes. | **PASS** | Lab `CR=1.2 mg/dL`, value column = TEXT, no numeric coercion. |
| OQ-66 | Create offer → ACCEPT; create offer → DECLINE with reason; create offer → let expire. | All three transitions recorded with audit. | **PASS** | 3 offers created; transitions verified PENDING→ACCEPTED_PROVISIONAL, PENDING→DECLINED (reason `MEDICAL_CONDITION_INCOMPATIBLE`), PENDING→EXPIRED. |
| OQ-67 | Record transplant event; rejection episode; biopsy; readmission. | All recorded. | **PASS** | All four events recorded for synthetic patient `MRN-NRTC-PTX-001`. |
| OQ-68 | Create living donor; advance through evaluation steps; auto-tasks for 6/12/24 month follow-ups appear. | Yes. | **PASS** | Tasks generated at 6/12/24 month dates relative to transplant date. |
| OQ-69 | Ingest sample HL7 v2 ADT^A01 message. | Patient created or updated. | **PASS with note** | ADT^A01 from sample-data accepted; patient created. **Note:** an OBX-5 free-text field of 6 KB triggered a parser warning but the patient record was still created correctly. **Logged as DEF-003 in VSR.** |
| OQ-70 | Generate OPTN-style export. | CSV produced; filename and header carry "DO_NOT_SUBMIT" watermark. | **PASS** | Filename: `TCR-NRTC-DO_NOT_SUBMIT-20260605.csv`; line 1: `# DO_NOT_SUBMIT — TCR-shaped extract, not an OPTN/UNet submission`. |

## Reporting

| ID | Step | Expected | Pass/Fail | Evidence |
|---|---|---|---|---|
| OQ-120 | Generate HIPAA audit report by user + date range. | Report contains all audit rows in scope. | **PASS** | 14-day report for `coordinator-2` returned 87 rows, matched DB query. |
| OQ-121 | Show migration status. | Lists applied & pending. | **PASS** | Admin diagnostics: applied=27, pending=0. |
| OQ-122 | Show encryption status & rotation history. | Visible. | **PASS** | Status=Enabled, AES-256-CBC, PBKDF2-HMAC-SHA512, history shows the 2026-06-05 rotation row from OQ-42. |

## Cross-cutting

| ID | Step | Expected | Pass/Fail | Evidence |
|---|---|---|---|---|
| OQ-140 | Submit oversized patient identifier. | Rejected with validation error. | **PASS** | 600-char `patient_id` rejected: "Patient ID must be 64 characters or less". |
| OQ-141 | Capture egress with PCAP for 30 min normal use. | Only whitelisted hosts. | **PASS** | PCAP `oq141-egress.pcapng`: only DNS, NTP, syslog destinations (no Internet egress). |
| OQ-142 | Inspect audit log row for `request_id`. | Present and unique. | **PASS** | Random sample of 50 rows: each `request_id` is a v4 UUID and unique. |
| OQ-143 | Open About dialog. | Design alignment statement present (not "certified"). | **PASS** | Dialog text: "TransTrack v1.2.0 — designed for alignment with HIPAA Security Rule and 21 CFR Part 11; not certified." |

## Acceptance

100% of OQ test cases marked Mandatory must pass. Failures are recorded
as defects in the customer's defect tracker and resolved before VSR
sign-off.

**Result: 38/41 PASS, 3 PASS-with-note (DEF-001, DEF-002, DEF-003 — all
Severity 3, all functionally non-blocking, all carried to VSR for
accepted-risk sign-off). 0 Mandatory failures.**

| Role | Name (placeholder) | Signature | Date |
|---|---|---|---|
| Executor | _Sarah Chen, RN_ | _signed_ | 2026-06-05 |
| Reviewer | _Marcus Johnson, MS_ | _signed_ | 2026-06-09 |

---

> *End-of-document reminder:* the test environment, signatures, and
> defect IDs above are demonstration data.
